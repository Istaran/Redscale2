var cache = require('./cache');

let getControls = async function (state) {
    let controls = [];
    // Add cards based on phase, etc
    console.log("Tried to get combat card controls, but it wasn't implemented.")
    return controls;		
}

module.exports = {
    getControls: getControls
};