
let update = async function (state, details) {
    // TODO: All buildings are subject to raids, but hoards count their workers' work stats as a bonus to security, allowing them to safely hold more.
    // Your main hoard has a further bonus to security based on you being you.
}

module.exports = {
    update: update
};